theme: jekyll-theme-architect

repository: partitio/grpc-gateway

collections:
  docs:
    output: true

defaults:
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-toc

exclude:
  - run.sh
